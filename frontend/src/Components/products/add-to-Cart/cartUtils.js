export const SHIP_THRESHOLD = 500;
export const fmt = (n) => Number(n).toLocaleString("ro-RO");
export const fmtRate = (n) => n.toLocaleString("ro-RO", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
